#pragma once

#include "menu_item.hpp"
namespace Menu{
    const MenuItem* show_menu(const MenuItem*);
    const MenuItem* exit_func(const MenuItem*);
    const MenuItem* go_back_func(const MenuItem*);
    const MenuItem* learn_addition(const MenuItem*);
}
